# Fixture: one obviously fake hardcoded credential, one correct variable use.
# The scanner must flag the first and stay silent on the second.

### Leaky request
GET {{base_url}}/echo
Authorization: Bearer sk_test_faketoken1234567890abcdef

### Clean request
GET {{base_url}}/echo
Authorization: Bearer {{auth_token}}
